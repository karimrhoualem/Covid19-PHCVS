@page
@model C19VS.Pages.AgeGroup.AgeGroupInsertModel
@{
}

<form method="post" class="mt-3">
    <div class="form-group row">
        <label asp-for="AgeGroup.ageGroup">Age Group</label>
        <div class="col-sm-10">
            <input asp-for="AgeGroup.ageGroup"/>
        </div>
    </div>

    <div class="form-group row">
        <label asp-for="AgeGroup.Allowed">Allowed</label>
        <div class="col-sm-10">
            <input asp-for="AgeGroup.Allowed"/>
        </div>
    </div>

    <div class="form-group row">
        <div class="col-sm-10">
            <button type="submit" class="btn btn-primary">Save</button>
            <a asp-page="/AgeGroup/AgeGroupIndex" class="btn btn-primary">Cancel</a>
        </div>
    </div>
</form>